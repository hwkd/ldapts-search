{
  "name": "ldapts-search",
  "version": "1.0.0",
  "description": "Search through active directory",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["ldap", "active directory", "typescript"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "ldapts": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^11.9.4",
    "typescript": "^3.3.3"
  }
}